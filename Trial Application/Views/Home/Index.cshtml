<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Helping Hands Foundation</title>

<!-- FontAwesome Icons -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<!-- Custom CSS -->
<link rel="stylesheet" href="/css/landing.css" />

<style>
/* Reset margin/padding */
        *html, body {
            margin: 0;
            padding: 0;
            width: 100%;
            overflow-x: hidden; /* prevent horizontal scroll */
        }


/* Hero Section */
        .hero {
            width: 100%;
            height: 100vh;
            background-image: url('/images/kids-bg.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
            position: relative;
        }


.hero .overlay {
    position: absolute;
    width: 100%;
    height: 100%;
    background: rgba(34, 139, 84, 0.55); /* semi-transparent green overlay */
    top: 0;
    left: 0;
    z-index: 1;
}

.hero-content {
    position: relative;
    z-index: 2; /* ensure content is above overlay */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 20px;
    width: 100%;
    max-width: 800px;
}

.hero-content .small-title {
    font-size: 18px;
    margin-bottom: 10px;
}

.hero-content h1 {
    font-size: 48px;
    margin-bottom: 20px;
    line-height: 1.2;
}

.hero-content .sub-text {
    font-size: 20px;
    margin-bottom: 30px;
}

.learn-btn {
    background: #2ecc71;
    color: white;
    padding: 12px 30px;
    border-radius: 8px;
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
    transition: 0.3s;
}

.learn-btn:hover {
    background: #27ae60;
}

/* Responsive adjustments */
@@media (max-width: 768px) {
    .hero-content h1 {
        font-size: 32px;
    }
    .hero-content .sub-text {
        font-size: 16px;
    }
    .learn-btn {
        font-size: 16px;
        padding: 10px 25px;
    }
}
</style>
</head>

<body>

    <!-- Hero Section -->
    <header class="hero">
        <div class="overlay"></div>

        <div class="hero-content">
            <p class="small-title">Get Started Today.</p>
            <h1>Let's Help Beings!</h1>
            <p class="sub-text">
                With so much to consume and such little time, your help makes a difference.
            </p>

            <a href="#" class="learn-btn">Learn More</a>
        </div>
    </header>

</body>
</html>
